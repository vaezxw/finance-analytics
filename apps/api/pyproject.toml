[project]
name = "finance-analytics-api"
version = "0.1.0"
description = "FastAPI query API on Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
